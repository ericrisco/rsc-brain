# Exact historical findings only. A fingerprint binds commit + path + rule + line; it cannot hide
# the same value in a later commit or another file.
# Reason: two old commits introduced the non-secret Hunt idempotency replay marker; the current
# fixture now uses an obvious low-entropy value that the generic API-key rule does not classify.
4fd836add73645c85131b14704242ee4fad52f92:tests/integration/test_console_hunting_skills.py:generic-api-key:124
c7ae55a849855f9037f359f9cedd8d713cb0b970:tests/integration/test_console_hunting_skills.py:generic-api-key:124
# Reason: these are the two original API-key detector assertions from test_doctor.py, before their
# current lines gained a reasoned line-local gitleaks marker.
9cc99438b0211551ce27005822cccacd2c081560:tests/unit/test_doctor.py:generic-api-key:13
9cc99438b0211551ce27005822cccacd2c081560:tests/unit/test_doctor.py:generic-api-key:30
# Reason: AUDIT-142. `data/first-admin-credential` — a generated first-admin password for
# `admin@rsc-brain.local` — was committed on 2026-07-25 in 82b03a9 and has been public since. The new
# `rsc-brain-generated-password` rule now recognises that format, and this fingerprint records the one
# already-published occurrence so the rule fails on any NEW one. The value belongs to a local
# development database that no longer exists; it is not in use by any deployment. Purging it from
# history would need a force-push, which is the repository owner's decision and not a scanner's.
82b03a93537f87d7acea4d12f5433798b53ac262:data/first-admin-credential:rsc-brain-generated-password:2
# Reason: AUDIT-142's own detector fixture, and NOT a credential for anything. The first version of
# the `rsc-brain-generated-password` rule's positive assertion used a realistic 24-character token, so
# gitleaks' default `generic-api-key` rule flagged it — correctly. The fixture is now the lowest-entropy
# string of the same shape, but the commit that carried the first one is published: it was pushed to a
# branch that has since been closed and deleted, and GitHub retains a closed PR's head ref permanently,
# so `fetch-depth: 0` still reaches it. An abandoned branch is not a rewritten one. Fingerprinted here
# for the same reason as the four fixtures above, with the same limit: this binds one commit, one path,
# one rule and one line, so the value has no exemption anywhere else.
2d393b1a4a66e945beb87259d83476900b86ac67:tests/unit/test_no_tracked_credentials.py:generic-api-key:154
