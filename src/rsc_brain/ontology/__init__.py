"""Optional per-project ontology anchoring (SPEC-24, D17 / FR-17.x): OWL/RDF/SKOS management,
entity anchoring, bounded rdfs:subClassOf + skos:broader/narrower inference, and RDF export. Off by
default; the ontology NEVER decides permissions (tags stay the sole, deterministic gate, FR-17.8)."""
