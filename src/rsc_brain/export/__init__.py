"""Open-format export (SPEC-22, FR-10.6, D14): an OKF bundle of active claims + skills, respecting
the exporting principal's permissions. OKF lives only at the boundary — never as internal storage.
"""
